# fill in this function to greet the user!
my_list=[]
def greeting(user_info):
    my_list = user_info.split()
    return("Hello, " + my_list[0] + "! I also enjoy " + my_list[2] + "!")
print(greeting("Andy 24 programming"))
